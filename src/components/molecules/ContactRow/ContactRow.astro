---
import Link from "@components/atoms/Link/Link.astro";

type Props = {
  /** Field label. Displayed in monospace uppercase. */
  label: string;
  /** Contact value — plain text, or the link label when `href` is provided. */
  value: string;
  /** Optional link target. `https://` URLs are treated as external (new tab, `rel` added); `mailto:` URLs are not. */
  href?: string;
  /** Optional supplementary note rendered below the value in smaller muted text. */
  note?: string;
};

const { label, value, href, note } = Astro.props;

const isExternal = href ? /^https?:/.test(href) : false;
---

<dl class="contact-row">
  <dt>{label}</dt>
  <dd>
    {
      href ? (
        <Link href={href} external={isExternal}>
          {value}
        </Link>
      ) : (
        value
      )
    }
    {note && <small>{note}</small>}
  </dd>
</dl>

<style>
  .contact-row {
    display: grid;
    grid-template-columns: 8rem 1fr;
    gap: var(--space-s);
    margin: 0;
    padding-bottom: var(--space-m);
    border-bottom: 1px solid var(--border);
  }

  .contact-row:last-child {
    border-bottom: 0;
  }

  dt {
    font-family: var(--font-mono);
    font-size: 0.7rem;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--text-subtle);
    padding-top: 0.4rem;
  }

  dd {
    margin: 0;
    font-size: var(--step-1);
    font-family: var(--font-display);
    font-weight: 500;
  }

  dd small {
    display: block;
    margin-top: 0.2rem;
    font-family: var(--font-sans);
    font-size: var(--step--1);
    font-weight: 400;
    color: var(--text-muted);
  }
</style>
